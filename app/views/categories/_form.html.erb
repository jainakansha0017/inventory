<%= form_for(@category) do |f| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
      <% end %>
      <div class="row">
          <div class="col-xs-4"></div>
              <div class="col-xs-8">
                  <div class="form-group"> 
                      <div class="row">
                          <div class="col-xs-3"><strong>Category Name *</strong></div>
                          <div class="col-xs-4">
                            <%= f.text_field :name, type:"text", class: "form-control", placeholder: 'Enter category name' , required: "true" %>
                          </div>
                          <div class="col-xs-4"></div>
                      </div> 
                    </div>
                    <div class="form-group"> 
                      <div class="row">
                          <div class="col-xs-3"></div>
                          <div class="col-xs-4">
                            <%= f.button show_text, :class => 'btn btn-primary btn-block', id: "submit_btn" %>
                          </div>
                      </div> 
                    </div>
              </div>
       </div>
    


<!--   <div class="actions">
    <%= f.submit %>
  </div> -->
<% end %>
